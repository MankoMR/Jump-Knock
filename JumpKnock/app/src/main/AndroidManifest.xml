<?xml version="1.0" encoding="utf-8"?>
<!--
 *Copyright (c) 2020 Manuel Koloska, All rights reserved.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.band.jumpknock">

    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MainTheme">
        <activity android:name=".CalibrateActivity" android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".ShowRecordsActivity"
            android:theme="@style/RecordTheme" />
        <activity
            android:name=".RecordActivity"
            android:theme="@style/GoodTheme" />
        <activity android:name=".GameActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>